"""Scenario generation: perturbed price paths, seeds, resampled returns.

Step 2 of the build order. No TradingAgents dependency here — test
this module standalone before wiring it into the worker loop.
"""
